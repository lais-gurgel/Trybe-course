// Faça um programa que, dado um valor n qualquer, seja n > 1, imprima na tela uma pirâmide com n asteriscos de base.
//BABY STEPS
// Criação de variavel N, e simbolo a ser impresso
// Criacao de for para percorrer a matriz e variaveis correspondentes

let base = 5;
let symbol = "*";
let lineIndex;
let columnIndex;

for (lineIndex = 0; ; lineIndex += 1) {
    for (columnIndex = X; regra; columnIndex += 1) {

    }
}
